---
name: ai-engineering
description: Work on AI and machine-learning projects, experiments, models and datasets.
triggers: ai, machine learning, model, dataset, llm, training, evaluation
required_permissions: project.read, project.write, terminal.execute
---
# AI Engineering
Inspect the project and environment before changes. Keep experiments reproducible, record configuration, validate results, and never expose credentials in logs or memory.
